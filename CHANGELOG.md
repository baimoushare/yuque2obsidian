# CHANGELOG

本文件记录对功能、使用、维护和发布有实际影响的版本变化。

## v0.7.0 - 2026-09-01 | 运行时安全与发布链路升级

### Added

- Windows 登录 Cookie 使用当前用户 DPAPI 加密保存，并自动迁移历史明文 Cookie。
- Windows 构建支持可选代码签名，并为发布 EXE 生成 SHA-256 清单。
- 新增安全回归测试，覆盖浏览器沙箱、外部资源地址过滤和凭据隔离。
- 自动化测试重新纳入版本库，作为导出、画板、登录、加密和 Obsidian 行为的回归基线。

### Changed

- 桌面配置、Cookie、日志、崩溃报告和默认输出迁移到用户数据目录，避免安装目录不可写和个人数据混入程序目录。
- 桌面端与复杂内容工作进程改用标准输入传递任务配置，避免 Cookie 和密码暴露在进程环境变量中。
- 增量导出增加来源版本与目标路径校验，源文档变化后不再错误跳过。
- Markdown 与 JSON 输出改为临时文件写入和替换，并在覆盖已有文件前保留备份。
- 加密块无法重新加密时改用隐私占位符，不再回退为明文导出。
- Puppeteer、Axios 和 Cookie 依赖升级，并明确要求 Node.js 22.12 或更新版本。
- 项目统一使用 npm 和 `package-lock.json`，移除 Yarn 锁文件。

### Fixed

- 修复外部资源下载可能携带语雀 Cookie 或 CSRF 信息的问题。
- 修复外部资源地址可能指向本机、内网或带凭据 URL 的风险，并限制重定向和最大下载体积。
- 修复 Windows 默认关闭 Chromium 沙箱的问题；仅允许 Linux 用户显式选择关闭沙箱。
- 修复配置文件损坏时桌面端无法启动的问题，损坏文件会保留后回退安全默认值。
- 修复窗口关闭后 Node.js、浏览器或导出子进程可能继续驻留的问题。
- 修复复杂内容工作进程可把结果文件写到任务目录之外的问题。

### Docs

- README 补充运行环境、用户数据目录、隐私边界、浏览器依赖、签名方式和已知限制。
- 新增本 CHANGELOG，并采用三段式版本号和固定分类记录后续正式版本。

### Infra

- 打包产物不再包含 Cookie、桌面配置或开发者本机绝对路径。
- EXE 发布改为保留上一版本、分阶段校验和失败回滚，降低损坏包覆盖可用版本的风险。
- Python 运行与打包依赖改为固定版本，提高 Windows 构建可复现性。
- 本地运行缓存、验证目录、日志和备份文件统一加入 Git 忽略规则。
