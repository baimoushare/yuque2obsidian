# v0.8.2 版本总结：OTA 异常恢复加固

- 版本：v0.8.2
- 日期：2026-09-01
- 发布主题：让 OTA 网络失败和启动回滚保持可恢复
- 交付状态：本地代码与测试已完成；需生成 v0.8.2 上传包，远端宝塔仍待上传验收。

## 重要变化

### 问题修复

- 更新检查遇到 HTTP、TLS 或网络错误时会落入可重试的错误状态，不再永久停留在“正在检查”。
- 新版健康检查超时或进程异常退出时，helper 会先结束仍占用新版 EXE 的进程，再恢复 `.previous`，降低 Windows 文件替换失败概率。

### 测试与发布

- 新增网络失败后再次检查的回归测试。
- 新增回滚前结束失败进程的回归测试。
- 当前版本源为 v0.8.2，构建后发布包应上传至：
  `C:/wwwroot/update.baimoushare.cn/yuque2obsidian/releases/v0.8.2/`

## 验证记录

- Python 自动化测试：22/22 通过。
- Node 自动化测试：203/203 通过。
- Python、JavaScript 语法检查通过。
- `npm audit --audit-level=high`：0 vulnerabilities。
- v0.8.1 冻结 EXE 启动和 OTA manifest 验签已通过；v0.8.2 重新构建与上传包验签待本轮完成。
- 远端 `stable/manifest.json`、`manifest.sig` 和 v0.8.1 包地址当前均返回 HTTP 404，宝塔上传仍是外部前置。
