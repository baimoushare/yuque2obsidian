# 语雀画板图形导出升级验证记录

> 记录日期：2026-08-31
> 状态：代码、自动化测试、真实语雀画板重新导出、全新隔离 Obsidian 实例加载、结构校验和红框区域视觉复核均已通过；正式 Vault 未打开、未改动。

## 1. 已验证结果

- BoardIR 能从真实 CG 样本读取 46 个节点、49 条连线，结构与布局完整度均为 100%。
- 生成的 Excalidraw 场景通过反向读取校验：46 个图形节点、46 个绑定文本、49 条箭头、49 条双端绑定箭头，元素 ID 无重复。
- 独立复杂画板会生成 `.excalidraw.md` 主文件；嵌入式复杂画板在原 Markdown 位置提供“打开可编辑流程图”入口。
- 同源重复导出不重写；来源变化且未人工改动会先备份到 `history/` 再更新；检测到人工改动会生成 `.yuque-update.excalidraw.md`，不覆盖原文件。
- PNG 截图完成后会补充写入对应 `*.manifest.json` 的 `generatedFiles`，并记录截图状态。
- 完整结构化的 Markdown/Markmap/Mermaid 在默认 `fallback-only` 策略下不再无条件截图；布局敏感、部分结构化或无法结构化的画板仍保留 PNG 回退。
- 已实测目标 Vault 启用了 Markmap、Excalidraw 与 Excalidraw Extras；其 CLI Vault 注册检测为未确认。因此标准 Excalidraw 文件只依赖插件启用，CLI 不再阻断生成。
- 桌面端已实际启动并检测到“图形导出”下拉项；历史 `desktop.settings.json` 的 UTF-8 BOM 读取问题已修复。真实设置读回为 `diagramExportMode=auto`、`diagramSnapshotMode=fallback-only`。

## 2. 自动化验证

```text
npm test
193 passed / 0 failed

py -m unittest discover -s test -p 'test_*.py'
7 passed / 0 failed

git diff --check
通过（无空白错误）
```

覆盖内容包括：Excalidraw 生成与回读、原始 points/path 路径保留、无原始路径时直线回退、嵌入式编辑入口、无 CLI 时的插件分流、同源幂等、自动更新备份、人工修改保护、截图清单同步、Obsidian 插件能力检测、桌面设置默认值/归一化/UTF-8 BOM 兼容，以及同方向一对多/多对一边中心和混合方向连接测试。

## 3. 复杂流程图结构证据

样本：`runtime-validation/obsidian-excalidraw-20260819-092007/验收-复杂流程图.excalidraw.md`

```text
节点 46 / 46
连线 49 / 49
绑定文字 46 / 46
起点绑定 49 / 49
终点绑定 49 / 49
结构错误 0
```

校验结论：元素 ID 无重复；每个绑定文字的 `containerId` 均指向有效节点；49 条箭头的起点和终点绑定均指向有效节点。新生成器已取消共享锚点分散逻辑：同方向连线统一使用该方向边中心，混合方向连线分别使用正确边中心；有原始 `points/path` 时保留中间折点，无原始路径时只生成端点直线；相关回归测试通过。

## 4. 真实语雀重新导出与隔离 Obsidian 运行时证据

### 4.1 真实语雀重新导出

- 来源：`https://www.yuque.com/baimoushare/biji/cp8x212migdtk29z`。
- 画板：`CG制作丨流程图`。
- 隔离导出目录：`runtime-validation/real-yuque-cg-20260829/`。
- 原始基线文件：`runtime-validation/real-yuque-cg-20260829/CG制作丨流程图.excalidraw.md`；本轮正式验收文件见 4.3.1。
- Manifest 来源类型：`real-yuque-public-api`；来源哈希和生成哈希已写入 `manifest.json`。
- 结构统计：46 个节点、49 条连线、46 个绑定文字、49 条双端绑定箭头、0 个结构错误。
- 语雀 API 返回的 49 条连线均未提供可恢复的多段 `points/path`；生成器因此默认采用“中心锚点 + 端点直线回退”，仅对会穿过其它节点的两条汇线追加外部正交避障路径。

### 4.2 隔离 Obsidian 运行时

- 隔离 Vault：`runtime-validation/obsidian-excalidraw-20260819-092007`。
- 隔离用户数据：`runtime-validation/obsidian-user-data-20260828`。
- 隔离 CDP 端口：`9237`。
- `app.vault.getName()` 已确认是 `obsidian-excalidraw-20260819-092007`。
- `obsidian-excalidraw-plugin` 已加载，`community-plugins.json` 已包含该插件。

### 4.3 真实语雀画板视觉复核

- 隔离 Vault：`runtime-validation/obsidian-real-yuque-cg-20260829`。
- 隔离用户数据：`runtime-validation/obsidian-user-data-real-yuque-20260829`。
- 隔离 CDP 端口：`9238`。
- 插件已在隔离实例中真实加载，文件已从 Markdown 视图切换到 Excalidraw 画布。
- 全画布截图：`runtime-validation/real-yuque-cg-20260829/obsidian-real-yuque-cg-20260829-excalidraw.png`。
- 局部截图：`red-box-top-mid.png`、`red-box-mid-late.png`、`red-box-regions-zoom.png`。

复核结论：

- 上部 `概念方案→故事设定/人物设定/场景设定→风格/色彩/美术`：旧版连续回环和多段折返消失；同一源节点从右边中心连接，视觉明显改善。
- `Layout→动态分镜/基础灯光氛围/低模制作` 及 `低模制作→人物低模/场景低模/道具低模`：旧版折返消失；各组连线从统一边中心发出。
- `建模→人物高模/场景高模/道具高模`：旧版节点边缘分散连接消失，当前为统一右边中心的直线分流。
- `后期→音效/色调/剪辑`：旧版折返消失，三条线从统一右边中心发出。
- `人物高模/材质UV/动作设计/面部捕捉/布料结算/毛发→角色动画`：目标端均绑定到 `角色动画` 左边中心；`材质UV→角色动画` 和 `毛发→角色动画` 改为从中间节点右侧外部通道绕行后汇入目标，已避开 `动作设计`、`面部捕捉`、`布料结算`。
- 中文节点、箭头和分支均存在；截图确实显示 Excalidraw 画布，不是 Markdown 源文。

当前判断：中心锚点修正和无原始路径时的节点外部正交避障规则均已生效。最新真实导出文件已在隔离 Obsidian 的 Excalidraw 画布中打开并完成红框区域复核。

几何验收补充：对最新 `CG制作丨流程图-避障验收.excalidraw.md` 的 49 条箭头逐段检查，排除每条连线自身的起点和终点节点后，与其它节点矩形的相交数为 `0`；角色动画区域两条外部绕行线的通道横坐标约为 `1066`，中间节点右边界约为 `976`，保留约 `90` 像素净距。

### 4.3.1 2026-08-31 最终独立复验

- 真实来源再次通过公开只读 API 获取：`https://www.yuque.com/baimoushare/biji/cp8x212migdtk29z`，返回 `CG制作丨流程图`、`Board/lakeboard`；解析结果为 46 个节点、49 条连线。
- 实时原始响应存档：`runtime-validation/real-yuque-cg-20260829/CG制作丨流程图.live-yuque-20260831-011316.json`。
- 本次正式验收导出文件：`runtime-validation/real-yuque-cg-20260829/CG制作丨流程图-避障验收.excalidraw.md`；覆盖前已生成同目录 `.before-live-rerun-20260831-011316.bak` 备份。
- 回读场景：141 个元素、46 个节点、46 个绑定文字、49 条箭头、49 条双端绑定箭头、重复 ID 为 0；`elbowed=0`，多点避障箭头为 2 条。
- 最终隔离 Vault：`runtime-validation/obsidian-final-visual-vault-20260831-141418`；最终隔离用户数据：`runtime-validation/obsidian-final-visual-user-20260831-141418`；CDP 端口：`9242`。该实例与正式笔记 Vault 完全隔离。
- 在该全新实例中，用户手动信任隔离 Vault 并启用 Excalidraw 插件；先确认已有 `最终-v3` 变体可渲染，随后实际打开本次正式重导文件 `CG制作丨流程图-避障验收.excalidraw.md`。CDP 快照显示 Excalidraw 工具栏和 `绘制 Canvas`，证明不是 Markdown 源文或插件失活页。
- 正式文件全画布截图：`runtime-validation/real-yuque-cg-20260829/obsidian-live-final-20260831-official.png`；正式文件角色动画局部截图：`runtime-validation/real-yuque-cg-20260829/obsidian-live-final-20260831-official-role-animation-crop.png`。`obsidian-live-final-20260831-full.png` 及其局部图仅保留为 `最终-v3` 变体的先行渲染证据。
- 局部视觉结论：`材质UV→角色动画` 与 `毛发→角色动画` 均先在 `角色动画` 左侧外部通道汇合，再短水平进入目标；没有穿越 `动作设计`、`面部捕捉`、`布料结算`，没有回环、折返或边框贴线。

### 4.4 复杂图视觉渲染

- 已真实打开并渲染 `验收-复杂流程图.excalidraw.md`。
- 截图证据：`runtime-validation/complex-flowchart-render-20260828.png`。
- 截图已人工检查：中文节点、分支和箭头完整可见，缩放约 48%；不是空白页、插件失活页或加载错误页。

### 4.5 绑定拖动与关闭重开

- 在隔离 Vault 中真实拖动“审核”节点，`y` 坐标由 `60` 变为 `223.15918987450112`。
- “审核”文字的 `containerId` 仍为 `rb619481e2198`，并随节点移动。
- 两条原有箭头仍保持 `开始→审核`、`审核→完成` 的双端绑定。
- 关闭并重新打开后，坐标、文字容器绑定和两条箭头绑定均保持。
- UI 操作过程中 Excalidraw 额外产生 1 条未绑定箭头 `n2zp36pK`；该元素不属于导出结果，已如实保留，未删除隔离文件内容。

## 5. 安全边界

- 正式 Obsidian Vault `C:\Users\www77\AppData\Roaming\obsidian` 未操作。
- 未执行正式语雀全量导出；本次仅对公开可读的单一真实画板进行隔离重新导出与验收。
- 未覆盖正式 Vault 中已有 Excalidraw 文件。
- 未执行 commit、push、建分支或依赖树批量升级。
- `npm install` 暴露的既有依赖审计风险未使用 `npm audit fix` 自动处理，以避免未经确认的大范围升级。

## 6. 当前仍需关注的事项

- 工作区仍包含本轮及历史用户改动，不能表述为“干净”；交付时应以 `git status --short` 如实列出。
- `n2zp36pK` 是隔离 UI 操作产生的额外未绑定箭头，不应被误判为导出结构缺陷；后续若需清理，必须单独获得确认。
- 本记录证明当前方案和隔离样本验收通过，不等于已对正式语雀数据执行全量导出。

## 7. 结论

当前已具备进入正式导出前评审的证据：自动化测试通过、真实语雀重新导出结构达到 46/49 门槛、同方向中心锚点回归测试通过、混合方向回归测试通过、原始路径优先回归测试通过；且正式避障文件已在全新隔离 Obsidian 用户数据与 Vault 中真实渲染并完成全画布/局部红框视觉复核。红框区域的旧版回环、边缘分散和穿过中间节点的长直线均已解决；本轮真实画板视觉验收通过。正式语雀全量导出仍需另行确认，本轮未执行。
